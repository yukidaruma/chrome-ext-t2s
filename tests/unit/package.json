{
  "name": "@extension/unit-tests",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier . --write --ignore-path ../../.prettierignore --cache --cache-location node_modules/.cache/.prettiercache"
  }
}
